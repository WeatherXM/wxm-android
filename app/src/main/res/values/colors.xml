<?xml version="1.0" encoding="utf-8"?>
<resources>
    <color name="colorPrimary">@color/light_primary</color>
    <color name="colorPrimaryVariant">@color/light_darkest_blue</color>
    <color name="colorOnPrimary">@color/light_top</color>

    <color name="colorSurface">@color/light_top</color>
    <color name="colorSurfaceVariant">@color/light_light1</color>
    <color name="colorOnSurface">@color/dark_background</color>
    <color name="colorOnSurfaceVariant">@color/light_dark_grey</color>

    <color name="colorBackground">@color/light_background</color>
    <color name="colorOnBackground">@color/dark_background</color>

    <color name="successTint">@color/light_success_tint</color>
    <color name="errorTint">@color/light_error_tint</color>
    <color name="warningTint">@color/light_warning_tint</color>
    <color name="midGrey">@color/light_mid_grey</color>
    <color name="darkGrey">@color/light_dark_grey</color>

    <!-- System and Launcher -->
    <color name="ic_launcher_background">#1B75BA</color>
    <color name="status_bar_color">#80003b77</color>

    <!-- List Divider -->
    <color name="list_divider">@color/light_light2</color>

    <!-- Google Analytics -->
    <color name="google_analytics_icon_background">@color/light_light2</color>

    <!-- Bluetooth Scanning -->
    <color name="progress_color_in_button">@color/light_lightest_blue</color>

    <!-- Dates Tabs -->
    <color name="date_tab_selected_background">@color/light_light2</color>

    <!-- History Charts -->
    <color name="chart_title_background">@color/light_light2</color>
    <color name="chart_popup_background">@color/light_darkest_blue</color>
    <color name="chart_popup_text_color">@color/light_top</color>
    <color name="chart_secondary_line">@color/light_lightest_blue</color>
    <color name="chart_fill_collor">@color/light_lightest_blue</color>

    <!-- Devices List -->
    <color name="devices_list_card_background">@color/light_light2</color>

    <!-- Claiming Flow -->
    <color name="select_device_card_color">@color/light_light2</color>

    <!-- Token Chart -->
    <color name="token_info_chip_background_color">@color/light_light2</color>

    <!-- Transactions -->
    <color name="token_transaction_hash_background">@color/light_blue_tint</color>

    <!-- Info Card -->
    <color name="info_card_background">@color/light_blue_tint</color>
</resources>
